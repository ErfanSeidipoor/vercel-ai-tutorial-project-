# AI SDK Core Embeddings

This project is based on the [AI SDK Core Embeddings](https://sdk.vercel.ai/docs/ai-sdk-core/embeddings#custom-headers)

# AI SDK Core: Embeddings Tutorial

The AI SDK provides the embed function to embed single values, which is useful for tasks such as finding similar words or phrases or clustering text.
